{
  "name": "scrollable",
  "version": "0.0.0",
  "description": "Scrollable components for advanced mobile scrolling with React.js",
  "main": "src/scrollable.js",
  "dependencies": {
    "react": "^0.13.0-beta.1"
  },
  "devDependencies": {
    "reactify": "^1.0.0",
    "watchify": "^2.3.0"
  },
  "scripts": {
    "build": "browserify -t reactify examples/main.js -o examples/build/bundle.js --insert-global-vars React",
    "watch": "watchify   -t reactify examples/main.js -o examples/build/bundle.js --verbose --debug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Irae Carvalho <irae@irae.pro.br>",
  "license": "MIT"
}
